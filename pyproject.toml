[project]
name = "flyte"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cachetools>=6.2.4",
]

[tool.uv.workspace]
members = [
    "test_automation_engine_stackstorm",
]
